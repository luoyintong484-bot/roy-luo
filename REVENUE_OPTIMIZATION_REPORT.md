# R7 Fortune 站内付费体系全面点评与优化建议

> 评审时间：2026-07-25  
> 视角：以可营收网站为标准，逐项诊断当前付费报告、定价策略、转化漏斗、技术安全  
> 结论：**架构底子好，但付费后体验和转化漏斗有多个直接漏钱的硬伤，优先级排序如下**

---

## 一、付费后报告体验（最高优先级）

### 1.1 付费成功页 1.8 秒就跳走 — 严重

**现状：** `PaymentSuccessPage.tsx:117` 里 `setTimeout(() => navigate(returnPath), 1800)`。

用户刚付了 ¥79-¥109，只看到 1.8 秒的"解锁成功"就被弹回报告页。这期间：
- 没有订单回执（金额、产品名、订单号、有效期）
- 没有"你已解锁了什么"的价值确认
- 来不及截图保存（尤其手动收款模式）
- 没有任何后续引导（查看报告 / 再测一个 / 分享给朋友）

**用户心理：** 花 ¥109 买完报告，1.8 秒后回到原来那个页面，第一反应是"我是不是没付成功？"

**建议：**
- 成功页停留 **5-8 秒**，或改为手动点击"查看报告"按钮才跳转（保留自动跳转作为兜底）
- 展示**订单回执卡**：产品名、金额、订单号、解锁有效期、返回路径
- 展示**已解锁内容清单**："你已解锁：命宫身宫 / 事业财帛 / 感情姻缘 / 健康提醒 / 流年节点 共 7 个模块"
- 添加**下一步引导**："查看完整报告" + "再测一个合盘" + "分享给朋友"

### 1.2 解锁有效期只有 7 天 — 太短

**现状：** `payment-service.ts:27` `DEFAULT_UNLOCK_DAYS = 7`

花 ¥79 解锁的本命盘报告，7 天后就过期了。用户花了真金白银，报告不能永久看？这在付费心理上非常负面，会导致：
- 用户觉得"被割韭菜"
- 差评和客服投诉
- 复购意愿下降

**建议：**
- 单次报告解锁改为 **30 天**（或永久）
- 如果要做订阅制（月度会员），那是另一个产品，不要混在单次购买里
- 7 天有效期只适合"免费体验"或"限时预览"

### 1.3 "分享可免费解锁 1 次" — 说了但没做

**现状：** `ReportLock.tsx:105` 文案写着 `Pay ${price.display} for permanent access | Share to unlock 1 free view`，但代码里没有任何分享解锁的实现。

用户看到"分享可免费解锁"，点击后什么都不发生 → 信任崩塌，直接流失。

**建议：**
- **要么实现它**：加一个分享按钮，分享后调用 `unlockReport(reportKey, 1)`（1 天有效期），让用户尝到完整报告的甜头
- **要么删掉它**：如果短期内做不了，先把文案改掉，不要承诺做不到的事

### 1.4 锁定内容在 DOM 里可被查看 — 安全漏洞

**现状：** `ReportLock.tsx:70` 锁定状态下 `children` 仍然被渲染在 `<div className="max-h-[280px] overflow-hidden">` 里，只是用 CSS `max-height` + 渐变遮罩隐藏。

任何用户打开浏览器开发者工具 → 删除 `max-h-[280px]` → 付费内容全部暴露。或者直接查看页面源码，所有报告文本都在 DOM 里。

**建议：**
- 锁定状态下**不渲染 children**，只渲染一个预览摘要 + 解锁按钮
- 真正的付费内容由后端 API 返回（或至少用条件渲染 `{isUnlocked && <Content />}`）
- 这是所有付费内容站的底线

---

## 二、定价策略与产品结构

### 2.1 两套互相矛盾的定价体系

**现状：**
- `PRODUCTS`（USD）：singleDraw $1.99, monthlyMember $12.99, aiDeepReading $30.83, cpReport $10.00
- `CNY_PRICES`（人民币）：tarot ¥29.90, ziweiTarot ¥39.90, natal ¥79.00, synastry ¥109.00, cp ¥69.90
- `PAYWALL_CONFIGS`：tarot 29.90, ziweiTarot 39.90, synastry 109.00, natal 79.00, cp 69.90
- `PaymentPage.tsx` 的价格总览用的是 `PRODUCTS`，但实际付费走的是 `PAYWALL_CONFIGS`

用户在付款页看到"月度会员 $12.99"，但实际点进去付的是 ¥29.90 的塔罗解读，金额和产品名对不上。

**建议：**
- 统一为一套定价体系（以人民币为主，海外用户自动换算）
- `PaymentPage.tsx` 的价格总览改用 `CNY_PRICES`
- 废弃 `PRODUCTS` 里的 USD 定价或仅用于海外展示

### 2.2 没有套餐/捆绑销售

**现状：** 每个报告独立售卖，用户要分别付 ¥79 + ¥109 + ¥69.90 = ¥257.90 才能看全本命盘 + 合盘 + CP 报告。

**建议：**
| 套餐 | 原价 | 套餐价 | 节省 |
|---|---|---|---|
| 本命盘 + 合盘 情侣套餐 | ¥188 | ¥159 | ¥29 |
| 全站畅读月卡 | - | ¥199/月 | 全部报告无限看 |
| 首次体验价 | ¥79 | ¥39.9 | 50% off |

捆绑销售是提升客单价最直接的方式。

### 2.3 月度会员的价值不清晰

**现状：** `monthlyMember` 定价 $12.99/¥93.53，描述是"月度會員 · 無限次抽牌+完整解析"，但：
- 没有独立的会员介绍页
- 不清楚包含哪些报告类型
- 不清楚和单次购买的关系
- 在付款页和塔罗解读混在一起

**建议：**
- 做一个独立的会员权益页，明确列出：包含哪些报告、每月可用几次、是否含 AI 深度解读
- 月卡定价建议 ¥39.9-¥59.9（当前 ¥93.53 对国内用户偏贵，对标同类产品如测测星座 ¥28/月）

### 2.4 AI 深度占卜（$30.83/¥221）没有产品页

**现状：** `aiDeepReading` 在 `PRODUCTS` 里存在，但站内没有任何页面介绍或入口。

用户不知道这是什么、怎么用、和普通塔罗有什么区别 → 不会买。

**建议：**
- 要么做一个独立的产品落地页
- 要么暂时下架，不要在价格总览里展示一个没有入口的产品

---

## 三、转化漏斗

### 3.1 付费墙出现太早

**现状：** 本命盘报告只免费展示第 1 个 section（"排盘依据与计算范围"），立刻就出现付费墙。

**问题：** 第一个免费 section 是"明确本次报告实际使用的数据，避免把未计算项目写进结论"——这是给从业者看的元数据说明，不是给普通用户看的"钩子"内容。用户还没感受到报告的价值，就被要求付 ¥79。

**建议：**
- 免费预览改为 **2-3 个 section**：核心格局定性 + 事业运深度 + 感情特质（让用户看到"这个报告真的有料"）
- 第一个 section 必须是**有情感冲击力的内容**，不是计算说明
- 参考竞品：测测星座免费看前 30%，R7 目前免费只看约 12%

### 3.2 没有免费体验机制

**现状：** 除了报告第一段，没有任何免费引导用户进入付费漏斗的机制。

**建议：**
- 新用户注册送 1 次免费塔罗解读（用注册邮箱或手机号做防刷）
- 分享报告到社交媒体送 1 次免费预览（配合 1.3 的"分享解锁"）
- 每日免费运势（已有 DailyFortune，可以加一个"今日免费牌"引导到付费）

### 3.3 付费墙缺乏社会证明

**现状：** CP 报告页有 "{totalStars} fans have unlocked"（但这个数字可能是假的），其他付费墙没有任何社会证明。

**建议：**
- 在 ReportLock 下方加："已有 XXX 人解锁此报告"
- 加用户评价/评分（可以是初始种子数据）
- 加"最近解锁"的实时提示（"3 分钟前有人解锁了本命盘报告"）

### 3.4 只有支付宝，没有微信支付

**现状：** `PAYMENT_METHODS.cn` 只有 `alipay_h5`。

在中国市场，没有微信支付 = 流失 40%+ 的付费用户。这是最大的单一转化杀手。

**建议：**
- 优先接入微信支付（和支付宝共用同一个聚合支付通道即可）
- 如果短期内接不了，至少在付款页明确说明"暂仅支持支付宝"

---

## 四、技术安全

### 4.1 解锁状态纯前端存储 — 可伪造

**现状：** `r7_unlocked_reports` 存在 localStorage，"签名"只是 `reportKey + expiresAt + userAgent` 的 hash，任何人 30 秒内可以伪造。

**建议：**
- 短期：后端返回一个加密 token，前端只存 token，验证时请求后端
- 长期：所有付费内容由后端 API 返回，前端不持有任何解锁逻辑

### 4.2 IS_TEST = true — 海外支付仍是模拟

**现状：** `payment.ts:16` `IS_TEST = true`，虽然 `const.ts` 里 `TEST_MODE = false`。

这意味着海外用户走 Creem checkout 时，实际上走的是 `IS_TEST` 分支（模拟支付），不是真实支付。

**建议：**
- 上线前确认 `IS_TEST = false`
- 或者把海外支付也切到后端代理

### 4.3 grantBenefits 的会员判定有 bug

**现状：** `payment.ts:310` `params.amount >= 12.00` 判定是否为会员购买。

但 CNY 定价里 synastry ¥109 ≈ $15.14 > $12.00，会被误判为会员购买，给用户错误地开通 VIP。

**建议：**
- 不要用金额判定产品类型
- 用 `metadata.productType === "membership"` 明确标识
- 去掉金额 fallback 逻辑

---

## 五、报告内容质量

### 5.1 CP 报告内容是"文字墙"

**现状：** `CpReportPage.tsx` 的每个 section 是一大段没有分段的纯文本（单段超过 500 字），没有任何小标题、列表、数据卡片。

用户付 ¥69.90 看到的是一大坨文字，体验极差。

**建议：**
- 每段拆成 2-3 个小段
- 加小标题（`【元素共振】`、`【星宿安排】`）
- 加数据卡片（匹配度、元素组合、星宿关系）
- 参考本命盘报告的 `ReportSectionCard` 结构（有 highlight + body + bullets）

### 5.2 本命盘报告第一个免费 section 太无聊

**现状：** 免费展示的是"排盘依据与计算范围"，内容是"报告只解释排盘引擎已经返回的数据"。

**建议：** 免费展示改为第 2 个 section"命盘总览与核心格局定性"（有情感冲击力的内容），把"排盘依据"放到付费区或折叠到底部。

### 5.3 没有 PDF 导出

**现状：** 文档清单提到 PDF 按钮，但代码里没有实现。

付费用户期望能保存/打印报告。建议：
- 用 `html2canvas + jsPDF` 生成 PDF
- 或者提供"邮件发送完整报告"功能

---

## 六、优先级排序

| 优先级 | 问题 | 影响 | 工作量 |
|---|---|---|---|
| P0 | 付费成功页 1.8s 跳走 | 直接影响用户信任和复购 | 小 |
| P0 | 锁定内容在 DOM 可被查看 | 直接漏钱 | 小 |
| P0 | "分享解锁"说了没做 | 信任崩塌 | 小（改文案）/中（实现） |
| P0 | 7 天有效期太短 | 用户投诉 | 极小 |
| P1 | 两套矛盾定价 | 混淆 | 中 |
| P1 | 没有微信支付 | 流失 40% 付费用户 | 中 |
| P1 | IS_TEST 仍为 true | 海外支付不真实 | 极小 |
| P1 | grantBenefits 会员判定 bug | 错误开通 VIP | 极小 |
| P2 | 没有套餐/捆绑 | 客单价低 | 中 |
| P2 | 付费墙太早 | 转化率低 | 小 |
| P2 | CP 报告文字墙 | 体验差 | 中 |
| P2 | 没有社会证明 | 转化率低 | 小 |
| P3 | 月度会员不清晰 | 订阅转化低 | 中 |
| P3 | AI 深度占卜无入口 | 产品无法售卖 | 中 |
| P3 | 没有 PDF 导出 | 体验缺失 | 中 |
| P3 | 免费体验机制缺失 | 新用户转化低 | 大 |

---

## 七、立即可执行的优化清单

1. ✅ 改 `DEFAULT_UNLOCK_DAYS` 从 7 → 30
2. ✅ 改 `PaymentSuccessPage` 跳转时间 1800ms → 6000ms + 加订单回执
3. ✅ 删掉 `ReportLock` 里"分享可免费解锁"的虚假文案
4. ✅ 改 `ReportLock` 锁定时不渲染 children（防 DOM 泄露）
5. ✅ 修 `grantBenefits` 的金额判定 bug
6. ✅ 确认 `IS_TEST` 在上线前改为 false
7. 🔲 接入微信支付
8. 🔲 做套餐/捆绑定价
9. 🔲 改善 CP 报告排版
10. 🔲 加社会证明到付费墙
